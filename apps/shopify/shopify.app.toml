# Shopify app configuration (Shopify CLI "app config" format).
#
# client_id is real, registered at https://partners.shopify.com. The client secret is never
# checked in here — it lives only as the SHOPIFY_API_SECRET env var on the deployed service.
name = "pay-agent"
client_id = "92ea8f7b23d781c0d673f8adf1191cd8"
application_url = "https://shopify-production-19ce.up.railway.app"
embedded = true

[access_scopes]
scopes = "read_orders"

[auth]
redirect_urls = ["https://shopify-production-19ce.up.railway.app/auth/callback"]

[webhooks]
api_version = "2026-01"

[[webhooks.subscriptions]]
topics = ["app/uninstalled"]
uri = "/webhooks/app/uninstalled"
